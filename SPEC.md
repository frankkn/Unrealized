# UNREALIZED — 專案交接規格書

> **給接手的 Claude：** 這是一份完整的設計規格。設計討論已經結束，架構已經拍板，你要做的是實作。請先讀完整份文件再動手，並依「§10 開發階段」的順序進行。每完成一個階段就停下來讓我試玩、給回饋，不要一次寫完全部。

---

## §1 專案概要

**UNREALIZED** — 一款繁體中文（台灣用語）的互動式人生選擇網頁遊戲。

- 靈感來源是知乎那種「人生選擇」互動活動頁，但做得更深：加入**世代變數**與**性別變數**
- 玩家先選出生年代與性別，然後從童年一路走到晚年，每個階段做選擇
- 核心命題：**人生不只由你的選擇決定，還取決於你是哪一屆**
- 副標候選：`a Taiwanese life, in three generations`
- 中文名：`人生存摺`

**命名由來**：UNREALIZED 是會計術語「未實現損益」，同時是「未實現的可能」。它是唯一一個能讓最好的結局和最壞的結局共用同一個標題的字。

---

## §2 技術決策（要放上 GitHub）

### 2.1 原則

- **無建置流程**（no build step）。純 HTML + CSS + vanilla JS，直接開 `index.html` 就能玩，push 上 GitHub Pages 就能上線。不要引入 React／Vite／npm。
- **內容與引擎分離**。劇本資料放在獨立的 JS 檔，引擎完全不認識任何劇情內容。之後改劇本只動 data 檔。
- **一切純前端**，無後端、無 API、無追蹤。

### 2.2 檔案結構

```
unrealized/
├── index.html              入口，只有骨架與 mount point
├── README.md               專案說明（中英雙語，含玩法與設計理念）
├── LICENSE                 MIT
├── .nojekyll               讓 GitHub Pages 不跑 Jekyll
├── css/
│   └── style.css           全部樣式
├── js/
│   ├── engine.js           狀態機、屬性運算、結局判定、渲染
│   ├── state.js            存檔／圖鑑（localStorage 讀寫）
│   └── ui.js               DOM 操作、動畫、章節轉場
└── data/
    ├── config.js           世代設定、詞彙字典、屬性定義
    ├── nodes-ch0-3.js      第 0–3 章節點
    ├── nodes-ch4-5.js      第 4–5 章節點
    ├── nodes-ch6-7.js      第 6–7 章節點
    └── endings.js          全部結局（含中途結局）
```

> 用 `<script>` 逐一載入，不用 ES module，避免 `file://` 協定的 CORS 問題——這樣使用者下載 zip 後雙擊也能玩。全域命名空間統一掛在 `window.UNREALIZED` 底下。

### 2.3 localStorage

**這個專案要用 localStorage**，用途是結局圖鑑與設定記憶。這與「Claude 對話中的 artifact 不能用 localStorage」的限制無關——那是 claude.ai 沙箱的限制，這個專案跑在 GitHub Pages 上，localStorage 完全正常。

儲存的 key：

```
unrealized:codex        已解鎖結局 { endingId: { count, generations[], genders[], firstAt } }
unrealized:settings     { reducedMotion, textSpeed }
unrealized:lastRun      上一局的世代／性別，方便快速重玩
```

要有「清除紀錄」按鈕。讀寫全部包 try/catch（隱私模式下 localStorage 可能拋錯，不能讓遊戲整個掛掉）。

---

## §3 狀態模型

### 3.1 五軸屬性

範圍 0–10，起始值 5，**全程對玩家隱藏**，只在結局揭曉。

| key | 名稱 | 代表 |
|---|---|---|
| `money` | 財務 | 錢、資產、負債 |
| `achieve` | 成就 | 職位、名聲、專業能力 |
| `bond` | 關係 | 家人、伴侶、朋友 |
| `health` | 健康 | 身體與心理 |
| `self` | 自我一致 | 你做的選擇有多接近你真正想要的 |

`self` 是最重要的一軸。它讓遊戲能做出「你什麼都有了，但那不是你要的」這種結局，比單純的成功／失敗有味道。

### 3.2 旗標（flags）

字串集合，用來 gate 節點變體與結局。

```
出身   書香 勞動 單親 早熟 責任
求學   明星高中 技職 中離 頂大 喜歡的科系
中期   出國 北漂 西進 創業 接案 公職 移民 返鄉
關係   成家 有小孩 離婚 未婚 丁客 同性伴侶 已出櫃 未出櫃
陰影   借貸 高槓桿 投機 照顧 曾崩潰 壓抑 複製教養 疲勞駕駛 宗教金錢 家庭政治撕裂
時代   遇到風暴 錯過紅利 被取代
```

### 3.3 選項規則（原「沒有免費的選擇」鐵則，已取消）

**每個選項至少變動兩軸，且五軸加總不得超過 +3。** 選項**可以**是純粹的好事。

`dev/test-engine.js` 會掃過所有節點強制執行這兩點。

> **修訂紀錄一（實作後）**：最初寫的是「至少一軸必須下降」。那是**單邊約束**——只規定要扣、沒規定要給，於是 151 個選項平均淨值 −0.62，一局跑完五軸總和從 25 掉到中位數 13。後來改成約束淨值（上限 +1）。
>
> **修訂紀錄二（試玩後，本次）**：「提升必有犧牲」這條原則**整個取消**。
>
> 量出來的效果是：成就只有 24 個選項能升、卻有 43 個在扣，而自我一致有 51 個能升。結果是**幾乎每一個顧健康、顧家庭、慢下來的選項都在扣成就**。均衡玩到最後是「健康 10、自我 10、成就 1」——不是「什麼都不錯」，是「在乎生活就會毀掉事業」。那比原本想講的話強烈得多，也悲觀得多。
>
> 現在只留 +3 的上限，避免單一選項一次翻盤整局；「至少兩軸」保留，選擇才有質地，不會退化成一排 +1。

### 3.3.1 有回報的選項要「先投入過才看得到」

純加分的選項如果永遠可選，就變成明顯的正解，等於沒得選。所以這一類寫成**對前面投入的兌現**，用 `requires` 綁在屬性門檻上：

| 節點 | 選項 | 門檻 |
|---|---|---|
| `n5_career_move` | 你手上那個做了很久的東西，這一年終於做起來了 | `achieve >= 5` |
| `n6_career_plateau` | 以前的同事來找你，那邊剛好缺一個你這種資歷的人 | `achieve >= 5` |
| `n7_body_ledger` | 這些年走路、游泳、按時回診，到這個年紀開始領回來 | `health >= 6` |
| `n7_solo_aging` | 老朋友還在，而且這幾年變成固定每個月約一次 | `bond >= 6` |

**這是遊戲性上最重要的一條**：它讓「一直投資某件事」真的會有結果，而不是每一步都在還債。

### 3.4 時間本身的效果（章節基線）

鐵則管的是**選擇**，但人生還有選擇以外的那條基線：出社會之後薪水與年資本來就會長，到了晚年職位會還回去、身邊的人會慢慢散掉、身體會開始還債。

`config.chapterDrift` 定義每進入一個新章節自動套用的變化。少了這條，遊戲等於在說「人的一生是淨損耗」；有了它才是「人生有累積，只是代價也在累積」。

**`self`（自我一致）不在基線裡**——那一軸不會隨時間自己長出來，只能靠自己的選擇換。

基線刻意壓得比選項輕：**選擇仍然是主角，基線只是不讓一切單調下沉。**

### 3.5 鐵則：節點必須自證前提

**一個節點只有在它的敘述對這一局成立時，才可以出現。**

這條是實作後補的，因為同一個形狀的 bug 咬了四次，而且**從遊戲裡看不出來**——畫面不會壞，只會荒謬：

| 節點 | 症狀 |
|---|---|
| `n6_marriage_crisis` | 97% 的局都出現，其中 **43% 的玩家根本沒有伴侶**。他們選了分手或一路單身，卻被告知「你的關係走到了分岔點」，並拿到三個全部假設對面坐著一個人的選項 |
| `n6_financial_reckoning` | 對從沒借過一毛錢的人說「這幾年欠的、借的、賭的，開始一筆一筆找上門」，還給他「催收電話開始一天打好幾次」的選項 |
| `n5_debt` | 「有一筆錢，你怎麼算都算不過來」對每個人都跑，而且三個選項有兩個會蓋上 `借貸`——**走過第 5 章就被貼上財務陰影**，於是第 6 章必定被清算 |
| `n5_parents_ill` | 三個選項有兩個是性別限定，**不論選什麼性別實際都只看得到兩個**，長照變成二選一 |

實務上分成三種處理，依節點而定：

1. **整個跳過**（`n6_marriage_crisis`、`n5_debt`）：用 `next` 的變體陣列做條件路由。前提不成立就繞過去，**章節本來就不該對每個人長得一樣**
2. **改寫敘述與選項**（`n6_financial_reckoning`）：前提成立時是「清算」，不成立時是「四十幾歲的某個晚上第一次把帳攤開來算」，並把「催收電話」加上 `requires`
3. **補足選項**（`n5_parents_ill`）：性別限定的選項之外，要有雙方都看得到的

> **旗標會傳染。** 上面四個裡有三個的根因是「一個節點無條件蓋旗標，下游節點再讀那個旗標」。加旗標的選項要問一次：**這個旗標會不會讓後面某個節點誤以為前提成立？**

> **門檻會過期。** 婚變的條件原本是 `bond<=6 或 achieve>=7 或 照顧`。取消「提升必有犧牲」之後成就中位數從 1 變成 9，`achieve>=7` 於是 100% 成立；而長照節點無條件對每個人跑、三個選項有兩個蓋上 `照顧`。三個條件 OR 起來等於沒有條件——**有伴侶的人 100% 會遇到婚變**，那個事件也就不再是事件。
>
> 現在只用 `bond<=3`：一直維持關係的玩法 0%，真的疏忽了關係的 100%，兼顧的落在五成上下。
>
> **教訓**：門檻寫的是「當時的數值分布」，數值平衡一改它就可能失效，而且**失效的方向通常是變成永真**，從遊戲裡完全看不出來。改過平衡之後，要回頭量一次所有條件節點的觸發率。
>
> **`n6_long_term_care` 已處理**（同時用了「跳過」與「改寫敘述」兩種）：第 5 章選了送機構的人，第 6 章那個「你的生活開始繞著這件事打轉」對他不成立，整段跳過；選了請看護的人會看到另一版敘述（錢照付、人也在，只是沒有結束的一天）。`照顧` 旗標從近乎人人有，降到依玩法 7–56%。
>
> 順帶修掉引擎一個潛在的坑：`next` 的變體陣列現在可以**嵌套**（一段路由條件整段被另一段引用）。原本 `resolveNext` 只解一層，嵌套時會把整個陣列當成 nodeId 塞進 state，症狀是「跳到一個叫 `[object Object]` 的節點」，很難聯想到是路由組合出來的。

> **第五次：單身路線。** `n3_first_love/solo` 不蓋任何旗標，於是「一輩子沒有伴侶」在資料上不存在。選了它的人在第 5 章會被問「決定不婚，繼續在一起，但不進入法律關係」、「這段關係，最後還是走到了分開」，然後拿到 `成家`，再被問要不要生小孩，選項是「把資源留給彼此」。
>
> 處理方式是把「有沒有伴侶」變成明確的狀態（`單身` 旗標），並補上單身專屬的敘述與三個選項——**三十幾歲遇到一個人是真的會發生的**，所以那兩個選項會把 `單身` 拿掉。這需要引擎支援 `unflags`：旗標原本只能加不能減，下游只好去猜「有 A 但也有 B 所以其實是 C」。
>
> **第六次（自己造的）：三個節點原本 100% 觸發。** `n4_mlm`（有人來推銷）、`n5_emigrate`（要不要移民）、`n6_midlife_unemployment`（中年失業）都是「不見得會發生在你身上」的事，但每一局都跑。加門檻之後分別落在 37% / 59% / 33%。
>
> 其中 `n6_midlife_unemployment` 第一版門檻寫成「有 `被取代` 或 `遇到風暴` 旗標」，但 `被取代` 對 2005 世代是 100%（第 4 章那個節點三個選項都會蓋）——**又是一個永真的門檻**，整個世代必定失業。改成先看成就：`achieve<=5` 躲不掉，`achieve>=8` 過得去，中間那一段才由旗標決定。

> **第七次：把「固定支出」當成「缺口」。** `n5_debt`（算不過來的那筆錢）的門檻是 `money<=4 或 高槓桿 或 有小孩 或 照顧`。後三個**單獨就能放行**，於是事業穩定、收入很好、只是生了小孩的人也會被告知「有一筆錢，你怎麼算都算不過來」，還拿到「辦了現金卡，先撐過去」——29% 的觸發發生在 `money>=6` 的局，而在「事業穩定＋收入好＋有小孩」這種玩法上是 **100%**。
>
> 小孩、長照、房貸是**固定支出**，缺口是「支出撞上薄的餘裕」。改成三段式（`<=4` 一定緊、`>=8` 一定撐得住、中間才由負擔決定）之後，那種玩法降到 0%。
>
> **這是同一個形狀的第三次**（婚變的 `achieve>=7`、失業的 `被取代`、這次的三個旗標）。共同的錯誤是**把「相關」當成「等於」**：有小孩的人比較可能缺錢，但那不是缺錢。門檻要寫的是節點敘述真正的前提，不是它的統計代理。

#### 3.5.1 第二種矛盾：節點該出現，但那段文字不成立

前提檢查問的是「這個節點該不該出現」。還有一種錯是**節點該出現，可是它講的事情在這一局不成立**——用關鍵字掃過所有敘述、對照當下的旗標，量出來六千局裡有這些：

| 位置 | 症狀 | 次數 |
|---|---|---|
| `n7_solo_aging` | 標題「老後獨居」、敘述「你現在一個人住」，但**對每個人都跑**——成家、有小孩、bond 9 的人也會被告知他獨居 | 555 |
| `n5_marriage/breakup_common`<br>`breakup_lgbt_1975` | 分手之後**不蓋任何旗標**，於是下一個節點照樣問他要不要生小孩、「把資源留給彼此」。跟單身路線同一個 bug，只是走另一條路進來 | — |
| `END_家裡還很熱鬧` | 條件只有 `bond>=6`，文字卻寫「孩子會回來」 | 24 |
| `END_一樣都沒放掉` | 同上，「孩子還會回來吃飯」 | 1 |
| `END_無子的晚年` | 文字寫「等你們兩個都老到不能自己來」，但單身路線的「認真算過一個人生養」也會走到這裡 | 113 |
| `n5_emigrate` | 「為了孩子的教育」對有伴侶但沒小孩的人不成立 | 937 |

處理方式：**老後是每個人都有的一段，獨居不是**——所以節點留著，改成兩種老後（標題改為「老後的日子」，兩組敘述兩組選項）。結局則讓 `ending.text` 也支援變體陣列：條件收緊會讓結局變得太難拿到，改寫成通用的又會失去具體的畫面，「同一個結局，兩種家裡」才對。

還有一類是**旗標組合本身不可能**：

- `1975` + `同性伴侶` + `有小孩`（8000 局裡 158 局）。那個年代同性伴侶要有小孩，現實上只有一條路——走過一段對外的婚姻。所以 1975 的同性伴侶不再看到「決定生小孩」，改成「家裡壓了很多年，你走進一段對外的婚姻，也有了孩子」，同時蓋上 `未出櫃` 與 `壓抑`。
- `移民` + `返鄉`（11%）。這個**不是** bug——移民之後為了父母搬回來是真的會發生的事。但那跟北漂回鄉不是同一件事，所以補了專屬敘述（「飛回來要轉兩趟，簽證還得重辦」）與專屬選項。

> 三個檢查都進了 `dev/test-engine.js`（6d 敘述矛盾、6e 旗標組合、加上 6c 觸發率）。**這一類只有量得出來**：畫面不會壞，只會荒謬，而且只在特定旗標組合下出現。

#### 3.5.2 章節分布：一份精算表的形狀

量過一輪之後最明顯的結構問題不是節點太多，是**分布**：

| | 一局走過幾個節點 |
|---|---|
| 0–22 歲（22 年） | **6.2** |
| 28–50 歲（22 年） | **17.8** |

人真正記得、真正形塑他的那幾年給一個畫面（15–18 歲只有一個節點），中年清算給十八個。**那是精算表的形狀，不是人生的**，而且是「整體讀起來很壓抑」的結構性原因——不是文字太悲觀，是遊戲把四分之三的篇幅花在結算上。

處理：少年時代加三個節點（那個老師 · 第一次真的失敗 · 第一筆自己賺的錢），中年三個節點補上它們真正的前提（那筆存款要真的有存款、超支要真的有位置在追、返鄉要真的離過鄉）。9.1 : 17.2，比例從 1:2.9 收到 1:1.9。

> **旗標不能只寫不讀。** 曾經有 12 個旗標蓋了之後沒有任何條件讀它——遊戲把「你是誰」記下來，然後再也沒提過，於是十二歲拿去唸書的孩子跟十五歲進工廠的人，到五十歲回望時讀到的是同一段字。`dev/test-engine.js` 的 6f 現在會擋住這件事。**這是投入產出比最高的一項**：不需要新節點、不需要新圖，只要在後段的敘述加變體。

#### 3.5.3 一個人生模擬器少不了的四樣

1. **朋友要自成一條線。** `bond` 把家人、伴侶、朋友混在一起，於是把心力放朋友、沒放家庭的人，數值上跟相反的人一模一樣。不加第六軸（會動到 UI、鐵則驗證、36 個結局、圖鑑），改用旗標串起來，由第 6 章與結局讀回來。
2. **要有好運。** 原本只有壞運氣（車禍、風暴、詐騙），一個好運都沒有——那不是人生，是刑期。但好運不該是天上掉下來的：它落在「手上剛好有東西可以被幸運到」的人身上，跟 §3.3.1 同一條原則。
3. **手足。** 在台灣，手足決定長照怎麼分、遺產怎麼算、誰被期待回家。原本只在長照的一個選項裡出現過一次，而且**獨生子女也看得到那個選項**。
4. **要能修復。** 五軸全是流水帳，二十幾歲弄壞的東西之後沒有任何節點能補回來——但真實的人生有「那一年」。前提是真的有東西要補，所以門檻看的是這一局留下的疤：隨機玩法 73%，一路顧關係的玩法 0%。

#### 3.5.4 健康為什麼會變成單行道（第二次）

健康軸修過一次（把「萬用扣點」的用法清掉），但問題沒有真正解決。用一個參數化的玩家模型量出來才看得清楚——`k` 是「有多聽從上進的直覺」，0 是完全隨便挑，1 是每次都挑最有企圖心的：

| k | 修之前健康歸零 | 修之後 |
|---|---|---|
| 0.0 隨便玩 | 8% | **3%** |
| 0.4 一般玩家 | 77% | **40%** |
| 1.0 全力上進 | 98% | **65%** |

修之前 k=0.2（只是略微偏好上進）就有 46% 歸零。**收支其實是平的**（+31/−35，26 個給血選項對 30 個扣血），所以不是數字太重——是**選擇壓力全部同向**：回血的選項清一色是「慢下來、去檢查、請假」，讀起來像放棄；而所有讀起來吸引人的選項都扣血。對照 bond（+87/−87），它的給血選項裡有一大堆是玩家本來就想選的。**健康是唯一一條「有趣的選擇永遠是傷它的」的軸。**

實際改的三件事：

1. **第 1、3 章原本一個回血選項都沒有**（11 個節點）。補的不是「多喝水」型的選項，是玩家本來就想選的：進校隊、跟朋友晃、當兵體能變好、下班時間誰都不給。
2. **最會扣血的節點是「身體的訊號」**——一個關於健康的節點，對 k=0.4 的玩家平均 −1.56。忽略的代價收斂，去檢查的回報放大。
3. **又抓到三個「萬用扣點」**：陪朋友把事情處理完、試著理解長輩、差點被詐騙後查證，全都在扣健康。這正是當初讓健康變成單行道的模式——作者需要一個代價、手邊沒有貼切的軸，就抓健康。

> **倒下不能是陷阱。** 健康見底時那個「沒有停」的選項，83% 的玩家會選它，然後遊戲直接結束。它原本寫成「沒有停，手上的事還沒交代完」——讀起來就是一個普通的「繼續」。**83% 的人選它，那不是選擇，是陷阱。** 現在標籤自己講清楚後果。

> **hard 層要有第三個出口。** 身體的判決是絕對的（這一點不改，`hard` 仍在最前面），但身體垮掉的人生不只一種寫法。原本只有兩個出口，於是「全力上進」的玩法 85% 拿到同一個結局。加了「病房裡有人」（health<=0 且 bond>=7，bittersweet）之後降到 71%，而且 37 個結局在隨機玩法下**全部出現得到**。

#### 3.5.5 tier 分布：問題不在順序

| | hard | whole | generation | flagCombo | attribute | fallback |
|---|---|---|---|---|---|---|
| 修之前 | 31% | 0% | **40%** | 24% | 5% | 1% |
| 修之後 | 23% | 0% | 34% | 35% | 7% | 1% |

`generation` 獨大的主因是**一個結局**：`END_22K的逆襲` 的條件是 `1990 + achieve>=6`，而成就中位數是 8–9 —— 在有資格的局裡命中 **83%**，等於「你是 1990」就會拿到它，把後面所有更具體的結局蓋掉。`竹科的股票`（1975 + money>=6）同理。

試過把 `flagCombo` 提到 `generation` 前面（理由是「窄的先判」），**結果更糟**：flagCombo 有 15 個結局，提前之後它自己吃掉 62%，而且把「登記那天」「竹科的股票」整個蓋掉。**tier 不均的原因是個別條件太鬆，不是順序。**

> **窮舉會爆炸。** 第 0–3 章原本是全枚舉，加到 10 個節點之後路徑數變成千萬級（每加一個四選項的節點就 ×4），跑一次好幾分鐘。改成抽樣 40000 局，並斷言**每一個選項都真的被走過至少一次**——那才是這個測試真正要保證的事，圖上走不走得到由靜態分析負責。

### 3.6 怎麼驗

前提是敘述層面的事，沒辦法完全自動化。但四件事可以，而且都已經在 `dev/test-engine.js` 裡：

- **靜態圖分析**：走不到的節點與斷鏈。條件路由寫錯（例如變體陣列少了兜底那一項）會在這裡爆
- **門檻觸發率**（6c）：加過門檻的節點，觸發率必須落在 5%–95% 之間。**0% 表示門檻寫壞了，100% 表示門檻等於沒有**，兩個都踩過
- **單身路線**（6b）：三個世代 × 兩種性別各跑一局全程單身，斷言婚姻與小孩節點不會露出任何預設有伴侶的選項
- **詞彙覆蓋**（7）：字典裡每一組都必須真的出現在腳本裡。曾經寫了 26 組、只有 6 組被用到——**字典查得到，但玩起來完全沒有時代感**

改過路由或平衡之後，還要用 `dev/find-paths.js` 重新產生稀有結局的證明路徑。**條件路由一改，寫死的路徑一定失效**。

> **`GAME_END` 要明講。** `applyOption` 原本把任何 falsy 的 `next` 都當成「正常結束」，於是一張寫壞的路由表（變體陣列自我參照 → `undefined`）會**安靜地把整局收掉，跑六千局都不出聲**，而症狀只是「那個節點怎麼都不出現」。現在只有字串 `'GAME_END'` 算結束，解不出節點就當場丟例外。

剩下的還是得靠模擬：跑幾千局，統計「進入節點 X 時，前提旗標成立的比例」。上面那些數字都是這樣量出來的，而不是玩出來的——**靠玩很難發現，因為你不會剛好玩到那個組合**。

---

## §4 世代設定

三個世代，同一個台灣的不同三十年。

| | 1975（民國64年生） | 1990（民國79年生） | 2005（民國94年生） |
|---|---|---|---|
| 童年 | 三代同住／眷村／農村，家裡講台語或方言 | 公寓、雙薪、鑰匙兒童 | 獨生子女，父母正好是1990那一代 |
| 升學 | 高中聯考、大學聯考，錄取率約三成 | 學測指考、錄取率破九成、學歷通膨 | 108課綱、學習歷程、考得上但學校可能倒 |
| 兵役（男） | 兩年 | 一年 | 四個月→2024恢復一年，正好卡到 |
| 出社會 | 1997亞洲金融風暴、台商西進、竹科起飛 | 22K、無薪假、太陽花 | AI衝擊、疫情世代、遠距常態 |
| 房子 | 還買得起（2003年前） | 眼睜睜看它漲走 | 已不在討論範圍 |
| 同婚 | 一輩子都在非法與汙名中 | 2019通過時29歲，剛好趕上 | 從小就知道這是合法的 |
| 演到 | 2025，50歲 | 2040，50歲 | 2055–2075，近未來推想 |

**世代互文（重要）**：2005 世代的父母設定成 1990 世代。玩過 1990 再玩 2005 的人，會發現自己在扮演上一輪主角的小孩。第 0 章的敘述要留下可辨識的線索，但不要明說。

### 4.1 詞彙字典機制（成本最低、效果最好）

劇本裡寫佔位符，引擎依世代替換：

```js
LEXICON = {
  '{升學考試}':  { 1975:'大學聯考',   1990:'學測指考',   2005:'分科測驗' },
  '{起薪}':      { 1975:'兩萬四',     1990:'二萬二',     2005:'三萬六（但房租一萬八）' },
  '{兵役長度}':  { 1975:'兩年',       1990:'一年',       2005:'一年' },
  '{通訊}':      { 1975:'家裡那台轉盤電話', 1990:'MSN', 2005:'訊息已讀不回' },
  '{娛樂}':      { 1975:'租錄影帶',   1990:'網咖包夜',   2005:'短影音刷到三點' },
  ...
}
```

至少做 25 組。這是讓時代感落地最便宜的方式。

### 4.2 三層變體機制（控制工作量的關鍵）

工作量必須是單一版本的 **1.6 倍**，不是 6 倍。靠三層：

1. **詞彙字典**（零成本）— 上面那個
2. **文本變體**（約三成節點需要）— 同一節點寫 2–3 版敘述，依世代／性別挑
3. **專屬節點**（每世代 3–4 個、每性別 2–3 個）— 真正只有某世代或某性別會遇到的重點戲

---

## §5 性別處理原則

**性別不開新支線，只改內容與門檻。** 同一個階段格子，男女看到不同版本。

- **男性**：兵役吃掉一到兩年、養家期待、情緒不被允許表達（`壓抑` 旗標）、升遷相對順
- **女性**：比同齡男性早一到兩年進職場（因為沒有兵役，這是真實差異）、婚育年齡壓力、面試被問結婚計畫、被預設為照顧父母的人、產後回職場

第 3 章是最重要的一次分歧：男性進兵役節點，女性進「提早兩年」節點。**兩邊都不是好處，是不同的成本。**

---

## §6 節點地圖

### 遊戲開始前

兩個選擇，不算章節：世代（3）× 性別（2）。

### 第 0 章 · 家庭起點（0–10歲）

單一節點，三選項。

| 選項 | 屬性 | 旗標 |
|---|---|---|
| 公教之家，重視讀書，家教嚴 | money+1 achieve+1 bond−1 | 書香 |
| 家裡做生意／做工，很早自己照顧自己 | money−1 health+1 self+1 | 勞動 早熟 |
| 媽媽一個人拉拔長大 | money−2 bond+1 self−1 | 單親 責任 |

### 第 1 章 · 國中（12–15歲）

三節點對應三種出身，各三選項。

| 節點 | 抉擇 |
|---|---|
| `n1_bookish` | 全力衝刺 / 偷偷養一個興趣 / 有天早上就是不想去學校 |
| `n1_labor` | 自己摸出讀書方法 / 跟著朋友晃 / 發現自己的手感天賦 |
| `n1_single` | 拼獎學金 / 開始打工分擔 / 把心事全收起來 |

**早期結局（僅1975開放）**：家中經濟崩潰 → 國中畢業進工廠 → `END_factory15`。1990 之後義務教育與童工法讓這條路關閉。**從第一章就讓玩家感覺到「有些結局只有某個時代才有」。**

### 第 2 章 · 十五到十八（15–18歲）

四節點：`n2_elite_high`／`n2_normal_high`／`n2_vocational`／`n2_no_school`

第四個在 1975 是常態選項，1990 是少數，2005 幾乎等於中離——三代語氣完全不同。

**本章起，每章都要提供「就在這裡收尾，看看我的存摺」。**

### 第 3 章 · 十八到二十二（性別分歧點）

先選路線（5節點）：頂大熱門／一般大學／喜歡的科系／專科就業／直接工作

再插性別專屬節點：

| | 節點 | 內容 |
|---|---|---|
| 男 | `n3M_military` | 兵役。achieve−1 money−1，依選項可得 bond+1 或 self+1；可得 `壓抑` |
| 女 | `n3F_headstart` | 同輩男生在當兵，你已在職場。achieve+1 money+1，但「家裡希望你找個穩定的」→ self−1 風險 |

再一個共用節點 `n3_first_love`（3選項）。**此節點需加入同性戀情變體**：

- 選項之一是「你喜歡的是同性」，取得 `同性伴侶` 旗標
- 接著的出櫃抉擇，三代結果差異巨大：
  - 1975：出櫃幾乎不可能，`未出櫃` → 長期 self−、bond−，開啟晚年的專屬苦澀結局
  - 1990：2019 同婚通過時你 29 歲，剛好趕上。「等了十年終於可以登記」這條線可以寫得很有力量
  - 2005：從小就知道這是合法的，衝突從法律轉移到家庭與職場
- **這是全遊戲最能體現「時代決定你有哪些選項」的設計，請認真寫**

### 第 4 章 · 二十二到二十八 · 第一份工作

共用節點，拆成兩題：

- `n4_job`「靠什麼過日子」（5）：大公司外商／公職／中小企業／接案創作／接家業
- `n4_where`「在哪裡落腳」（3）：留在家鄉／北漂／出國

> **修訂紀錄（實作後）**：原規格是單一節點 7 個選項。實作完發現那是把兩個獨立的問題塞進同一個清單——前五個回答「做什麼工作」，`北漂` 與 `留學` 回答的卻是「住在哪裡」，兩者根本不互斥（北漂進大公司是最常見的組合之一，原本的寫法反而選不出來）。
>
> 而且在「屬性全程隱藏、只能靠敘述判斷」的前提下，一次讀 7 個選項負擔過重——全遊戲 44 個節點裡有 39 個都是 3 個選項，只有這裡是 7 個。
>
> 拆成兩題之後每題都是 3–5 個，組合反而變多，時代感也更準（同一份工作在家鄉與在台北是兩種人生）。代價是多一個決策步驟。

世代專屬（各一個重點戲）：

| 世代 | 節點 | 抉擇 |
|---|---|---|
| 1975 | `n4_westward` | 公司要派你去東莞，去三年還是留下 |
| 1990 | `n4_22k` | 起薪二萬二，忍還是走；隔年遇無薪假 |
| 2005 | `n4_replaced` | 你的入門工作被模型吃掉，轉領域還是往上擠 |

女性額外變體：面試被問「有沒有結婚計畫」，答法三選一，影響 achieve／self。

**新增節點 `n4_mlm`（宗教／直銷）**：家人或大學同學把你拉進一個「改變人生的機會」或宗教團體。選項：投入／拒絕但關係破裂／半推半就借錢投入。可得 `宗教金錢`、`借貸`。這是「歸零」與「不再聯絡」的另一條入口。

### 第 5 章 · 二十八到三十五（最重的一章）

大部分毀滅結局的種子都埋在這裡。

| 節點 | 埋的東西 |
|---|---|
| `n5_career_move` | money／achieve 大幅波動 |
| `n5_marriage` | 成家／不婚／分手（同性伴侶者此處為登記與否，依世代開放） |
| `n5_children` | 有小孩／丁客／拖著沒決定（女性壓力加重） |
| `n5_house` | `高槓桿`。1975買得起、1990剛好錯過、2005已不在選單 |
| `n5_invest` | `投機`。ETF／融資當沖／完全不碰。**「歸零」入口** |
| `n5_parents_ill` | `照顧`。女性被預設為照顧者，男性被預設為出錢；另有請看護與送機構兩個雙性別選項 |
| `n5_body_signal` | 健檢紅字。**只給敘述暗示（如「你已經三年沒做健檢」），不顯示數值** |
| `n5_overwork` | health−、achieve+ |
| `n5_debt` | 現金卡／跟家人借／硬撐。**「雙卡人生」入口** |
| `n5_era_storm` | 1975遇1997金融風暴、1990遇疫情、2005遇2035推想事件 |
| `n5_emigrate` | **新增**。為了小孩教育／政治氛圍／工作機會舉家移民。`移民`。可通往 `END_異地扎根` 或 `END_兩邊都不是家` |
| `n5_accident` | **新增**。車禍。**絕對不能是純隨機懲罰**——只在持有 `疲勞駕駛`（前面選過連續加班後騎車／趕時間）或 health≤3 時才會出現，且前面要有伏筆。選項：自己的傷／撞到人的責任／對方全責但纏訟三年 |

### 第 6 章 · 三十五到五十 · 清算開始

本章特色：**前面的帳開始還。**

節點（8＋2新增）：事業高原／中年失業／教養（判定 `複製教養`）／長照黑洞／婚變／健康清算（health≤2 直接觸發倒下結局）／財務清算（法拍、催收）／重新調整

- **新增 `n6_politics`**：選舉、公投或某場社會運動，讓餐桌變成戰場。世代變體是關鍵——1975 與 2005 的立場預設不同，而 1990 卡在中間。選項：吵到不再往來／閉嘴吃飯／試著理解。可得 `家庭政治撕裂`，通往 `END_不再聯絡` 的另一條路
- **新增 `n6_return_home`**：離鄉多年後，父母老了、老家空了。選項：搬回去／把父母接來／繼續兩地跑。`返鄉`

### 第 7 章 · 五十歲之後（三個世代皆開放）

節點（6）：退休準備／與孩子的關係結算／詐騙電話／老後獨居／身體的餘額／回望

**2005 世代的推想層（2055–2075）**：超高齡社會、勞保、氣候、照顧機器人。**推想控制在「制度與環境變了，人的困境沒變」這個尺度，不寫科幻**，避免與另外兩代的寫實調性打架。

> **`n5_parents_ill` 修訂紀錄**：原本只有三個選項，其中兩個是性別限定（女性→自己照顧、男性→出錢），所以**不論選什麼性別，實際只看得到兩個**。長照是第 5 章最重的題目之一，卻是二選一，而且剩下那個「請看護」是純花錢、等於沒得選。已補上「送去機構，親戚開始在群組裡說你不孝」——付出的是社會壓力與關係，換回的是自己的時間與健康。

### 中途收尾（第2章起）

不能只是逃跑鍵，所以中途收尾有自己的五個結局，用當下年齡＋最突出屬性判定：

`未完的存摺`／`十八歲的自己會怎麼看`／`還在路上`／`太早停下`／`這樣就好`

最後一個是隱藏的——只有五軸都落在中段、且 self 高時停下才會出現。等於遊戲在說：**知道什麼時候停，本身是一種能力。**

### 節點總計

| 類別 | 數量 |
|---|---|
| 骨幹節點 | 47 |
| 世代專屬 | 9 |
| 性別專屬 | 6 |
| 新增題材節點 | 6 |
| 中途結局 | 5 |
| 完整結局 | 28 |
| **合計** | **101** |

---

## §7 結局表

### 7.1 毀滅類（存摺上蓋黑章）

| 結局 | 觸發條件 | 調性 | 世代 |
|---|---|---|---|
| 雙卡人生 | money極低＋`借貸` | 循環利息一路還到中年，人生沒有一天是自己的 | 1975/1990 |
| 法拍 | `高槓桿`＋失業 | 那間你以為是終點的房子，門上貼了紅單 | 1990 |
| 歸零 | `投機` 連續押注 | 帳戶剩四位數，餘生在還那個曾經有過的數字 | 全 |
| 倒在辦公室 | achieve高＋health歸零 | 五十歲的心導管，你的職稱沒有一個人記得 | 全 |
| 洗腎的日子 | 長期忽略健康 | 一週三次，每次四小時，時間被抽走 | 全 |
| 職災 | 技職／產線＋health低 | 身體是本錢，本錢用完了 | 1975/1990 |
| 長照黑洞 | `照顧`＋self極低 | 照顧父母十年，出來時自己也老了，人生停在三十五歲 | 1990最強 |
| 被騙走的晚年 | money中＋bond低＋晚年 | 一通電話，一輩子的積蓄 | 1975/1990 |
| 一個人走的 | bond歸零＋高齡 | 房東發現時已經三天 | 1975 |
| 不再聯絡 | `複製教養`或`家庭政治撕裂`＋bond低 | 孩子成年後沒再回來，你到現在不知道為什麼 | 全 |
| 每天一瓶 | `壓抑`＋bond低 | 沒有崩潰的一天，只有慢慢下沉的十年 | 全 |
| 那場車禍之後 | `n5_accident` 特定分支 | 賠不完的責任，或再也沒好過的身體 | 全 |
| 拉進去的人 | `宗教金錢`＋bond低 | 你把親戚朋友都拉了進去，現在沒人接你電話 | 全 |

### 7.2 苦澀但沒毀掉

| 結局 | 條件 | 調性 |
|---|---|---|
| 有得有失 | 各軸中等，有明顯犧牲項 | 你沒有一次全拿 |
| 循環的重量 | `複製教養` | 你變成了當年不喜歡的那種大人 |
| 靜靜的如果 | self低＋其他都不錯 | 該有的都有，只是不是你要的 |
| 差一年 | 時代旗標命中 | 你什麼都做對了，只是晚了一屆 |
| 沒被時代選中 | achieve低＋self高 | 每個選擇在當時都合理，是時代轉向了 |
| 兩邊都不是家 | `移民`＋bond低 | 那裡沒有你的過去，這裡沒有你的現在 |
| 一輩子沒說出口 | `未出櫃`＋高齡 | 訃聞上寫著「終身未婚」 |

> **§7 修訂紀錄（試玩後）**：真人試玩兩局都以「倒在辦公室」收場，回饋是「整體很壓抑」。量測證實了三件事：
>
> 1. **健康見底時，第 6 章只給一個按鈕。** `n6_health_reckoning` 在 health≤2 時其他選項都被 `requires` 擋掉，玩家面對的是一顆「倒下」按鈕，遊戲把方向盤搶走。已補上「請長假把身體救回來」（health+3 achieve−2 money−1）——倒下仍然在，但那是你選的，不是被判的。被迫倒下的比例從 21% 降到 0%。
> 2. **好結局沒有一個是給「把時間給了人」的。** 六個好結局全都要高成就、高財務或特定旗標；一個把關係放第一位的玩家，好結局與中性結局都是 **0%**。已新增 `END_家裡還很熱鬧`（bond≥7）。
> 3. **判定順序會讓廣條件的結局蓋掉窄條件的。** `差一年` 只憑時代旗標就觸發（而幾乎人人都會拿到一個），在 flagCombo tier 就把後面整個屬性 tier 攔死。已加上「真的沒撈到」的條件。`自己的路` 原本要求 `achieve≥6`，但那個門檻等於在說「照自己想要的活還得混出名堂才算數」，剛好是這個結局要反對的事，已移除。
>
> 調性分布（毀滅／苦澀／中性／好）：拼事業 34→14% 毀滅、顧家庭 0→51% 好、五軸均衡 26→47% 好。亂點仍然 81% 毀滅——亂玩本來就該收場難看。

### 7.3 好結局／中性

自己的路／大器晚成／自在獨行／療癒的循環／遲來的答案／異地扎根／登記那天（同性伴侶＋1990或2005＋bond高）／**剛好的人生**（隱藏稀有：五軸全落中段——比衝到極端難得多）

### 7.4 世代限定

- **1975**：西進的人（回來時故鄉不認識你了）／竹科的股票（賣早了／沒賣）／十五歲的工廠
- **1990**：三明治世代／22K的逆襲
- **2005**：被模型取代的那一年／領不到的勞保／無子的晚年／氣候移民

限定結局在圖鑑裡標明世代。圖鑑本身就在講一件事：**有些結局你這輩子解不到，因為你的時代沒給你那個選項。**

### 7.5 判定順序

引擎依序檢查，先中者勝：

1. **硬觸發**：health≤1、或 money≤1且有`借貸` → 直接進毀滅結局
2. **世代限定**：旗標命中
3. **旗標組合**：`複製教養`＋bond低、`照顧`＋self低…
4. **最突出屬性**：最高軸與最低軸的組合決定骨架
5. **兜底**：有得有失

骨架選定後，再依旗標插入 1–2 段個人化段落（「你母親活到看見你成家」「那間房子最後還是留住了」）。這樣寫作量可控，但每次玩看到的結尾文字不會一樣。

### 7.6 圖鑑稀有度

常見 → 少見 → 稀有 → 隱藏

---

## §8 內容紅線（不可協商）

1. **不寫自殺結局，不寫自殺方式。** 這不是題材輕重的問題。就創作而言自殺也是偷懶的收尾——死亡把後果一次結清，玩家不用面對任何東西。真正痛的是活下來。所有財務崩潰類結局往這個方向寫：**「從此在早餐店煎蛋還債到六十五歲，每天四點起床，沒有人知道你曾經有過八百萬」比跳樓難受十倍，而且玩家會記得。**
2. **不放求助專線資訊**（已與我確認過，不需要）。
3. **毀滅結局必須可預防、有伏筆。** 健康在掉的時候，敘述要出現「你已經三年沒做健檢」這類暗示，但不顯示數值。玩家事後回想會發現遊戲提醒過他。**純隨機懲罰是不合格的設計。**
4. **不做真實政治人物、政黨的指名褒貶。** 第6章政治撕裂節點寫家庭關係的斷裂，不寫哪一邊對。
5. 涉及同性戀情、單親、移工、身心障礙等題材，寫困境不寫獵奇，敘述者永遠站在角色這一邊。

---

## §9 視覺與寫作方向

### 9.1 視覺概念：存摺

介面是一本存摺／護照。每個選擇「蓋一個章」，走到最後翻開看你蓋出了什麼樣的人生。

- 紙感底色、印刷邊框、騎縫線、流水編號
- 蓋章動畫（scale + rotate，落下的重量感）
- 章節標籤、已蓋章的收集列
- 結局是一個大的圓形結案章
- **可以沿用我在這個對話裡給你的第一版 HTML 的視覺方向**，但避免 AI 設計的三種預設長相：奶油白＋高對比襯線＋赭紅、近黑底＋螢光綠、報紙式細線排版
- 深藍墨色 + 米黃紙 + 印章紅 + 金線是已經定調的方向，可微調
- 響應式到手機、鍵盤焦點可見、尊重 `prefers-reduced-motion`

### 9.2 寫作規範

- **一律台灣用語。** 學測、指考、分科測驗、公職、當兵、北漂、竹科、22K、勞保、現金卡、法拍、健檢
- 每個節點敘述 **2–4 句**，不超過 4 句。這是遊戲不是小說
- **不說教、不下結論。** 讓玩家自己感覺
- 第二人稱「你」，現在式或過去式一致
- 結局文字 3–5 句，允許苦澀，**不允許廉價的勵志收尾**
- 選項文字要具體，不要抽象。寫「跟朋友借二十萬，說好三個月還」，不要寫「向他人求助」

---

## §10 開發階段（照順序做，每階段停下讓我試玩）

### 階段一：引擎與骨架
- 檔案結構、`index.html`、CSS 基礎
- 引擎：狀態機、屬性運算、旗標、詞彙字典替換、`when` 條件判定
- 世代／性別選擇畫面
- 第 0–3 章完整節點（含兵役／提早兩年／同性戀情變體）
- 全部結局判定邏輯 + 5 個中途結局 + 至少 8 個完整結局
- localStorage 圖鑑 v1
- **可玩、可跑完、可看到結局**
- 停下讓我試玩調性

### 階段二：中段
- 第 4–5 章全部節點（含世代專屬、直銷宗教、移民、車禍）
- 補到 20 個結局
- 結局的旗標個人化段落

### 階段三：後段與收尾
- 第 6–7 章（含政治撕裂、返鄉、2005推想層）
- 28 個結局補完（實作後為 29 個，見 §7 修訂紀錄）
- 圖鑑完整介面（稀有度、世代標記、未解鎖以剪影顯示）

### 階段四：上架
- README（中英雙語，含玩法、設計理念、世代對照表）
- LICENSE (MIT)、`.nojekyll`
- GitHub Pages 設定說明
- dev-mode 驗證工具：掃出違反「沒有純加分選擇」鐵則的節點、找出無法到達的節點、列出從未被任何路徑觸發的結局
- Open Graph meta（分享時的預覽圖與文案）

---

## §11 已拍板、不要再問我的事

- 名字用 UNREALIZED，中文「人生存摺」
- 三個世代：1975／1990／2005
- 2005 世代**要**演到晚年，用近未來推想（不是收在四十歲）
- 開場先選性別，才開始
- 台灣用語
- 允許苦澀、允許很壞的結局
- 不寫自殺、不放求助專線
- 做結局圖鑑
- 毀滅結局要有伏筆

## §12 需要你回頭問我的事

- 具體結局文字的調性，如果你覺得某個結局有兩種寫法值得選
- 第 6 章政治節點的處理方式，寫完先給我看
- ~~2005 世代推想層的具體事件設定（2035 風暴、2055 制度），寫之前先跟我確認一次~~

> **2005 推想層已補（第 6–7 章，2040–2075）。** 原本只有三個節點對 2005 有專屬敘述，整個遊戲只出現過一個未來年份（2035）。現在補了八處，全部守住「**制度與環境變了，人的困境沒變**」這條線，沒有寫科幻：
>
> 職位在幾次自動化之後被重新定義了兩遍 · 不是精簡而是整個職能被系統接手 · 成長紀錄從出生就存在雲端 · 照顧機器人翻身翻得比人穩，但長輩要的不是那個 · 老家那一帶淹過兩次 · 孩子在另一個時區工作，畫面很清楚話題很少 · 這棟樓一半的門後面都是一個人 · 能延長的年份比上一代多，但沒人保證延長的是哪一段
>
> 同時把詞彙字典從 6 組實用補到 26 組全用。**這裡的坑是文法不是內容**：字典的值長短與詞性不一，塞進句子會出現「你查資料的工具是沒有」、「錄取率是考得上，但學校可能倒」、「演算法推給你的給你的答案兩極」、「你先問過的不是醫生，是聽醫生的」。句型要寫成**三個世代的值都各自成句**才行——測試只能保證佔位符被替換掉，不能保證替換後讀得通，得三個世代各印一次出來讀。

---

**現在開始階段一。**
